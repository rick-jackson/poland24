type User = {
  id: string;
  email: string;
  phone: string;
  firstName: string;
  lastName: string;
  city?: string;
  post?: string;
  viber?: string;
  telegram?: string;
};

export default User;
